import React from 'react';
import closeIcon from '../../../../assets/icons/ic-close.png';

function CloseIcon(props) {
    return <img src={closeIcon} alt='Fechar' {...props} />;
}

export default CloseIcon;
